# Power Prediction - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
pandas>=2.0.0
numpy>=1.24.0
pvlib>=0.10.0
requests>=2.28.0
matplotlib>=3.7.0
scikit-learn>=1.2.0

# Optimization (required for JS model calibration)
scipy>=1.10.0
tqdm>=4.65.0

# Optional: ERA5 weather data from Copernicus CDS
# Uncomment if using ERA5 as a data source
# cdsapi>=0.6.0
# xarray>=2023.1.0

